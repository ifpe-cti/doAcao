/**
 * import { TestBed, inject } from '@angular/core/testing';

import { SolicitacaoDeSangueService } from './solicitacao-de-sangue.service';

describe('SolicitacaoDeSangueService', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [SolicitacaoDeSangueService]
    });
  });

  it('should be created', inject([SolicitacaoDeSangueService], (service: SolicitacaoDeSangueService) => {
    expect(service).toBeTruthy();
  }));
});

 * 
 */

