/**
 * import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { ListagemTecnicosComponent } from './listagem-tecnicos.component';

describe('ListagemTecnicosComponent', () => {
  let component: ListagemTecnicosComponent;
  let fixture: ComponentFixture<ListagemTecnicosComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ ListagemTecnicosComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(ListagemTecnicosComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});

 * 
 */

