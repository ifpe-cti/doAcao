/**
 * import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { SangueDoadoComponent } from './sangue-doado.component';

describe('SangueDoadoComponent', () => {
  let component: SangueDoadoComponent;
  let fixture: ComponentFixture<SangueDoadoComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ SangueDoadoComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(SangueDoadoComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});

 * 
 */

