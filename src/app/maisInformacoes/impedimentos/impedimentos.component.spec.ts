/**
 * import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { ImpedimentosComponent } from './impedimentos.component';

describe('ImpedimentosComponent', () => {
  let component: ImpedimentosComponent;
  let fixture: ComponentFixture<ImpedimentosComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ ImpedimentosComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(ImpedimentosComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});

 */

