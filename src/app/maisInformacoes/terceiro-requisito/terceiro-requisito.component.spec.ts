/**
 * import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { TerceiroRequisitoComponent } from './terceiro-requisito.component';

describe('TerceiroRequisitoComponent', () => {
  let component: TerceiroRequisitoComponent;
  let fixture: ComponentFixture<TerceiroRequisitoComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ TerceiroRequisitoComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(TerceiroRequisitoComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});

 * 
 */

