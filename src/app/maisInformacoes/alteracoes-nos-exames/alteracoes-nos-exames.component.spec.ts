/**
 * import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { AlteracoesNosExamesComponent } from './alteracoes-nos-exames.component';

describe('AlteracoesNosExamesComponent', () => {
  let component: AlteracoesNosExamesComponent;
  let fixture: ComponentFixture<AlteracoesNosExamesComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ AlteracoesNosExamesComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(AlteracoesNosExamesComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});

 */

