/**
 * 
 * import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { PosDoacaoComponent } from './pos-doacao.component';

describe('PosDoacaoComponent', () => {
  let component: PosDoacaoComponent;
  let fixture: ComponentFixture<PosDoacaoComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ PosDoacaoComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(PosDoacaoComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});

 */

