/**
 * import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { FichasMedicasComponent } from './fichas-medicas.component';

describe('FichasMedicasComponent', () => {
  let component: FichasMedicasComponent;
  let fixture: ComponentFixture<FichasMedicasComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ FichasMedicasComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(FichasMedicasComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
 * 
 * 
 * 
 */


