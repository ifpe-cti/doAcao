/**
 * import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { RequisicaoDoExameComponent } from './requisicao-do-exame.component';

describe('RequisicaoDoExameComponent', () => {
  let component: RequisicaoDoExameComponent;
  let fixture: ComponentFixture<RequisicaoDoExameComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ RequisicaoDoExameComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(RequisicaoDoExameComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should be created', () => {
    expect(component).toBeTruthy();
  });
});

 * 
 */

