/**
 * import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { CadastroFlebomistaComponent } from './cadastro-flebomista.component';

describe('CadastroFlebomistaComponent', () => {
  let component: CadastroFlebomistaComponent;
  let fixture: ComponentFixture<CadastroFlebomistaComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ CadastroFlebomistaComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(CadastroFlebomistaComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});

 * 
 */

