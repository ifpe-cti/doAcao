/**
 * 
 * import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { VisualizacaoFichasMedicasComponent } from './visualizacao-fichas-medicas.component';

describe('VisualizacaoFichasMedicasComponent', () => {
  let component: VisualizacaoFichasMedicasComponent;
  let fixture: ComponentFixture<VisualizacaoFichasMedicasComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ VisualizacaoFichasMedicasComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(VisualizacaoFichasMedicasComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});

 */

