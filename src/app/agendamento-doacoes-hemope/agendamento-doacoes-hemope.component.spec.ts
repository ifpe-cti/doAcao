/**
 * import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { AgendamentoDoacoesHemopeComponent } from './agendamento-doacoes-hemope.component';

describe('AgendamentoDoacoesHemopeComponent', () => {
  let component: AgendamentoDoacoesHemopeComponent;
  let fixture: ComponentFixture<AgendamentoDoacoesHemopeComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ AgendamentoDoacoesHemopeComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(AgendamentoDoacoesHemopeComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
 * 
 */


