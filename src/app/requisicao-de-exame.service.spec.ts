/**
 * 
import { TestBed, inject } from '@angular/core/testing';

import { RequisicaoDeExameService } from './requisicao-de-exame.service';

describe('RequisicaoDeExameService', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [RequisicaoDeExameService]
    });
  });

  it('should be created', inject([RequisicaoDeExameService], (service: RequisicaoDeExameService) => {
    expect(service).toBeTruthy();
  }));
});
 * 
 */

