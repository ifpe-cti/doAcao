/**
 * import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { VisualizacaoFichaMedicaIndividualComponent } from './visualizacao-ficha-medica-individual.component';

describe('VisualizacaoFichaMedicaIndividualComponent', () => {
  let component: VisualizacaoFichaMedicaIndividualComponent;
  let fixture: ComponentFixture<VisualizacaoFichaMedicaIndividualComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ VisualizacaoFichaMedicaIndividualComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(VisualizacaoFichaMedicaIndividualComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});

 * 
 */

