/**
 * import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { VisualizacaoAgendamentosComponent } from './visualizacao-agendamentos.component';

describe('VisualizacaoAgendamentosComponent', () => {
  let component: VisualizacaoAgendamentosComponent;
  let fixture: ComponentFixture<VisualizacaoAgendamentosComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ VisualizacaoAgendamentosComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(VisualizacaoAgendamentosComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});

 * 
 */

