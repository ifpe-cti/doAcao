/**
 * import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { VisualizacaoRequisicaoExamesComponent } from './visualizacao-requisicao-exames.component';

describe('VisualizacaoRequisicaoExamesComponent', () => {
  let component: VisualizacaoRequisicaoExamesComponent;
  let fixture: ComponentFixture<VisualizacaoRequisicaoExamesComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ VisualizacaoRequisicaoExamesComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(VisualizacaoRequisicaoExamesComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});

 * 
 */

