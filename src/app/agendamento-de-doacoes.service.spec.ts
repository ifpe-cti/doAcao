/**
 * import { TestBed, inject } from '@angular/core/testing';

import { AgendamentoDeDoacoesService } from './agendamento-de-doacoes.service';

describe('AgendamentoDeDoacoesService', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [AgendamentoDeDoacoesService]
    });
  });

  it('should be created', inject([AgendamentoDeDoacoesService], (service: AgendamentoDeDoacoesService) => {
    expect(service).toBeTruthy();
  }));
});

 * 
 */

