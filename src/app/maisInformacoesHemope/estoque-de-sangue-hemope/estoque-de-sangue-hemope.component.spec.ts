/**
 * 
 * import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { EstoqueDeSangueHemopeComponent } from './estoque-de-sangue-hemope.component';

describe('EstoqueDeSangueHemopeComponent', () => {
  let component: EstoqueDeSangueHemopeComponent;
  let fixture: ComponentFixture<EstoqueDeSangueHemopeComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ EstoqueDeSangueHemopeComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(EstoqueDeSangueHemopeComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});

 */

