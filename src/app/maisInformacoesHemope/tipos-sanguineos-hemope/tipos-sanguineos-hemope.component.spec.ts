/**
 * import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { TiposSanguineosHemopeComponent } from './tipos-sanguineos-hemope.component';

describe('TiposSanguineosHemopeComponent', () => {
  let component: TiposSanguineosHemopeComponent;
  let fixture: ComponentFixture<TiposSanguineosHemopeComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ TiposSanguineosHemopeComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(TiposSanguineosHemopeComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});

 * 
 */

