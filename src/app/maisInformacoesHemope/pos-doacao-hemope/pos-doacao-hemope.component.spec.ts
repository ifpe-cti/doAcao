/**
 * import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { PosDoacaoHemopeComponent } from './pos-doacao-hemope.component';

describe('PosDoacaoHemopeComponent', () => {
  let component: PosDoacaoHemopeComponent;
  let fixture: ComponentFixture<PosDoacaoHemopeComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ PosDoacaoHemopeComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(PosDoacaoHemopeComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});

 * 
 */

