/**
 * import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { QuemNaoPodeDoarHemopeComponent } from './quem-nao-pode-doar-hemope.component';

describe('QuemNaoPodeDoarHemopeComponent', () => {
  let component: QuemNaoPodeDoarHemopeComponent;
  let fixture: ComponentFixture<QuemNaoPodeDoarHemopeComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ QuemNaoPodeDoarHemopeComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(QuemNaoPodeDoarHemopeComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});

 * 
 */

