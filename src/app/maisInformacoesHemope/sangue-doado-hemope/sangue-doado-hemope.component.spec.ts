/**
 * import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { SangueDoadoHemopeComponent } from './sangue-doado-hemope.component';

describe('SangueDoadoHemopeComponent', () => {
  let component: SangueDoadoHemopeComponent;
  let fixture: ComponentFixture<SangueDoadoHemopeComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ SangueDoadoHemopeComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(SangueDoadoHemopeComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});

 * 
 */

