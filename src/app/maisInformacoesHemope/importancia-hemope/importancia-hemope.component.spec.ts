/**
 * import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { ImportanciaHemopeComponent } from './importancia-hemope.component';

describe('ImportanciaHemopeComponent', () => {
  let component: ImportanciaHemopeComponent;
  let fixture: ComponentFixture<ImportanciaHemopeComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ ImportanciaHemopeComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(ImportanciaHemopeComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});

 * 
 */

