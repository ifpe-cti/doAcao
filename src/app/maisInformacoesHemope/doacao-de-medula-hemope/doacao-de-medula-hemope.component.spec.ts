/**
 * import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { DoacaoDeMedulaHemopeComponent } from './doacao-de-medula-hemope.component';

describe('DoacaoDeMedulaHemopeComponent', () => {
  let component: DoacaoDeMedulaHemopeComponent;
  let fixture: ComponentFixture<DoacaoDeMedulaHemopeComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ DoacaoDeMedulaHemopeComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(DoacaoDeMedulaHemopeComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});

 * 
 */

